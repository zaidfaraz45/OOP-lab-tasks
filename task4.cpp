#include <iostream>
using namespace std;

int main()
{
    int n;
    
    cout << "Enter the number of strings: ";
    cin >> n;

    string *strings = new string[n];

    for (int i = 0; i < n; i++)
    {
        cout << "Enter string " << i + 1 << ": ";
        cin >> strings[i];
    }

    for (int i = 0; i < n; i++)
    {
        if (strings[i][0] > )
    }

    return 0;
}